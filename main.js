const clay = [ "Clay", "Clay", "Clay", "Clay", "hi" ];
const toFireInKiln = [];

for (const toco of clay) {
   const mug = `${toco} coffee mug`
   toFireInKiln.push(mug)
};

console.log(toFireInKiln);